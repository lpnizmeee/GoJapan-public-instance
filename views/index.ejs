<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File List</title>
</head>

<body>
    <h1>Uploaded Files</h1>
    <ul id="file-list">
        <% files.forEach(file=> { %>
            <li>
                <a href="<%= file.s3Uri %>" target="_blank">
                    <%= file.filename %>
                </a>
                (Uploaded at: <%= file.uploadTime %>)
            </li>
            <% }); %>
    </ul>
</body>

</html>